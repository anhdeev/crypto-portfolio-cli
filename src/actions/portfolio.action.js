const repositories = require('../repositories')

class PortfolioAction {
    constructor() {
    }

    getLatest = async ({filePath}) => {
        try {
            // split stream

            return stock
        } catch (error) {
            throw error
        }
    }

    getLatest = async () => {
        try {


            return stock
        } catch (error) {
            throw error
        }
    }
}

module.exports = PortfolioAction